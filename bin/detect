#!/usr/bin/env bash
# bin/detect <build-dir>

BUILD_DIR=$1

if [ -f "$BUILD_DIR/.gitattributes" ]; then
    printf "\n-----> .gitattributes detected, using Transcrypt buildpack\n\n"
    exit 0
fi

exit 1
